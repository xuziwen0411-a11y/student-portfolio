# 规划交接记录

记录 ID：

生成角色编号：1

生成角色名称：超级规划

生成时间（UTC）：

规划编号：

版本：

任务等级：

目标：

非目标：

前台范围：

后台范围：

底层/API 范围：

数据库与 Migration：

升级、兼容与回滚：

安全与资源身份：

主要风险：

测试矩阵：

验收标准：

规划偏差处理：

关联 Candidate SHA：不适用

目标状态：PLAN_AUDIT_PENDING

下一角色：2（超级审计）

下一句话：“规划已经OK了，去检查。”

## 用户可见正式里程碑回复

当前角色：1（超级规划）

当前状态：

标准主路径：1 → 2（方案审计）→ 3 → 2（Candidate 审计）→ 4

当前所在位置：

当前预计路径：

最低剩余主步骤：

下一步：

下一角色：2（方案审计）

推荐思考程度：极高

原因：规划需要核对范围、兼容、安全与验收的一致性

## 一键复制交接词

仅在确实需要用户转交时，另行生成包含接收角色、任务、GitHub 最新事实读取要求、准确规划与审计身份、允许范围和停止边界的自包含交接词。支持 Writing Block / 写作块时优先使用，否则使用独立代码块。
