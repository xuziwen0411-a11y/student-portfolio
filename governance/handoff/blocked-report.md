# 阻断报告

当前角色：

阻断编号：

阻断阶段：

发现的问题：

为什么必须停止：

已完成的安全动作：

当前生产是否受影响：

需要哪个角色接手：

建议下一步：

仓库状态：

## 用户可见正式里程碑回复

当前角色：

当前状态：BLOCKED

标准主路径：1 → 2（方案审计）→ 3 → 2（Candidate 审计）→ 4

当前所在位置：

当前预计路径：待阻断解除后重新推导

最低剩余主步骤：待阻断解除后重新计算

下一步：解除已记录阻断并重新读取可信事实

下一角色：按阻断记录中的责任角色；无法确定时为“待阻断解除后确认”

推荐思考程度：

原因：

## 一键复制交接词

仅在确实需要用户转交时，另行生成包含接收角色、阻断任务、GitHub 最新事实读取要求、准确阻断身份、允许范围和停止边界的自包含交接词。支持 Writing Block / 写作块时优先使用，否则使用独立代码块。
