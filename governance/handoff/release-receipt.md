# 正式发布回执

记录 ID：

生成角色编号：4

生成角色名称：超级发布

生成时间（UTC）：

发布编号：

版本：

已批准 Candidate SHA：

实际部署 SHA：

发布标签：

发布时间：

目标 Worker：已核对 / 不匹配

D1：已核对 / 不匹配

MEDIA_KV：已核对 / 不匹配

Secrets/变量：已核对 / 不匹配

发布前基线：

生产预检：

Migration 执行结果：

正式部署结果：

发布后冒烟验证：

原网址核对：

前台：

后台：

图片：

视频：

二维码/访问控制：

手机端：

回滚结果（如适用）：

最终状态：PRODUCTION_VERIFIED / ROLLED_BACK / BLOCKED

下一角色：结束 / 3（超级工作）/ 4（超级发布）

## 用户可见正式里程碑回复

当前角色：4（超级发布）

当前状态：

标准主路径：1 → 2（方案审计）→ 3 → 2（Candidate 审计）→ 4

当前所在位置：

当前预计路径：

最低剩余主步骤：

下一步：

下一角色：

推荐思考程度：

原因：

## 一键复制交接词

仅在确实需要用户转交时，另行生成包含接收角色、任务、GitHub 最新事实读取要求、准确获批 Candidate 与发布回执身份、允许范围和停止边界的自包含交接词。支持 Writing Block / 写作块时优先使用，否则使用独立代码块。
