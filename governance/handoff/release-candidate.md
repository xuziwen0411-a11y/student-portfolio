# Release Candidate 交接记录

记录 ID：

生成角色编号：3

生成角色名称：超级工作

生成时间（UTC）：

版本：

Candidate SHA：

分支：

基准提交：

完成的规划项：

主要改动：

新增 Migration：

数据库变化：

测试：

构建：

代码规范检查：

类型检查：

端到端测试：

浏览器/手机测试：

已知问题：

规划偏差：

生产环境修改：没有

目标状态：RC_AUDIT_PENDING

下一角色：2（超级审计）

下一句话：“候选做好了，去检查。”

## 用户可见正式里程碑回复

当前角色：3（超级工作）

当前状态：

标准主路径：1 → 2（方案审计）→ 3 → 2（Candidate 审计）→ 4

当前所在位置：

当前预计路径：

最低剩余主步骤：

下一步：

下一角色：2（Candidate 审计）

推荐思考程度：

原因：

## 一键复制交接词

仅在确实需要用户转交时，另行生成包含接收角色、任务、GitHub 最新事实读取要求、准确方案审计编号、Base、Head、Tree、分支、PR、允许范围和停止边界的自包含交接词。支持 Writing Block / 写作块时优先使用，否则使用独立代码块。
