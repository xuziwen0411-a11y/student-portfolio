# 进度报告

当前角色：

当前阶段：

当前版本：

已完成：

正在进行：

剩余：

当前阻断：

是否需要用户操作：

仓库状态 revision：

下一步：

## 用户可见正式里程碑回复

当前角色：

当前状态：

标准主路径：1 → 2（方案审计）→ 3 → 2（Candidate 审计）→ 4

当前所在位置：

当前预计路径：

最低剩余主步骤：

下一角色：

推荐思考程度：

原因：

## 一键复制交接词

仅在确实需要用户转交时，另行生成包含接收角色、任务、GitHub 最新事实读取要求、准确任务或 Candidate 身份、允许范围和停止边界的自包含交接词。支持 Writing Block / 写作块时优先使用，否则使用独立代码块。
