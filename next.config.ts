import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Vinext application options. Cloudflare response headers are applied by cloudflare/worker-entry.js. */
};

export default nextConfig;
